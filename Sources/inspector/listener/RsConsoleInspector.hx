package inspector.listener;

import entitySystem.Message;
import entitySystem.MessageResult;
import entitySystem.Listener;

class RsConsoleInspector extends Listener{
    override function onEvent(aMessage:Message):MessageResult {
        if(aMessage.data==)
        return SUCCESS;
    }
}
